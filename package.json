{
  "name": "TDEM",
  "version": "1.1.1",
  "description": "Install, search and manage extensions for TweetDeck",
  "author": "Ben Dixon <me@nuro.sh>(https://nuro.sh)",
  "license": "MIT",
  "scripts": {
    "build": "rimraf .cache/ dist/ && parcel build src/manifest.json --no-source-maps --log-level 3",
    "dev": "rimraf .cache/ dist/ && parcel watch src/manifest.json --no-source-maps --log-level 3",
    "pack": "npm run build && crx pack > ./dist",
    "lint": "prettier --config .prettierrc.json --write '**/*.{js,ts,vue}'"
  },
  "staticPath": {
    "source": [
      "/src/fonts"
    ],
    "target": [
      "/dist/fonts"
    ]
  },
  "dependencies": {
    "vue": "^2.5.17",
    "vuetify": "^1.1.12"
  },
  "devDependencies": {
    "@vue/component-compiler-utils": "^2.1.2",
    "crx": "^3.2.1",
    "parcel-bundler": "^1.9.7",
    "parcel-plugin-bundle-visualiser": "^1.1.2",
    "parcel-plugin-clean-dist": "^0.0.6",
    "parcel-plugin-copy": "^1.0.2",
    "parcel-plugin-web-extension": "^1.3.1",
    "prettier": "^1.14.2",
    "rimraf": "^2.6.2",
    "vue-hot-reload-api": "^2.3.0",
    "vue-template-compiler": "^2.5.17"
  }
}
